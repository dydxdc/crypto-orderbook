[package]
name = "orderbook"
version = "0.1.0"
edition = "2024"

[dependencies]
slab = "0.4.11"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "linkedlist_cache"
harness = false
